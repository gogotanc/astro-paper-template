---
export interface Props {
  noPadding?: boolean;
  ariaHidden?: boolean;
}

const { noPadding = false, ariaHidden = true } = Astro.props;
---

<div class={`max-w-2xl mx-auto ${noPadding ? "px-0" : "px-6"}`}>
  <hr class="border-border" aria-hidden={ariaHidden} />
</div>
